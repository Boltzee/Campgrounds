<% layout('layouts/boilerplate') %>
	<h1 style="text-align: center;">Create a new campground</h1>;
	<form action="/campgrounds" method="POST">
		<label for="title">Add a title for the campground:</label>
		<input type="text" id="title" name="campground[title]">
		<br>
		<label for="price">Add a price</label>
		<input type="number" id="price" name="campground[price]">
		<br>
		<label for="places">Select a place</label>
		<select name="campground[location]" id="places">
			<% for(let place of cities) {%>
				<option value="<%=place.city%>"><%=place.city%></option>
			<%}%>
		</select>
		<br>
		<button>Submit</button>
	</form>
	<a href="/campgrounds">Back to all campgrounds</a>
